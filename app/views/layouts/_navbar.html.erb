<nav class="w-full min-h-10 bg-green-400 flex items-center justify-between py-4 px-6 sticky top-0" data-controller="navbar">
  <a href="<%= root_path %>"> <h1 class="text-3xl font-bold">Fast Chat</h1> </a>
  <a href="<%= rooms_path %>"> <h1 class="text-3xl font-bold">Video Rooms</h1> </a>
  <% if current_user %>
    <a data-action="navbar#toggleDropdown"><h4 class="text-2xl text-black"> <%= current_user.username.capitalize %> </h4></a>
    <ul class="hidden bg-gray-300 px-10 py-2 lg:mr-8 z-10" id="dropdownMenu">
      <li class="text-xl">My Posts</li>
      <li class="text-xl">Edit Account</li>
      <li class="text-xl">Sign Out</li>
    </ul>
  <% end %>
</nav>
